# TEMPLATE ONLY — never put real keys in this file.
# It is committed to a public repo. Real values belong in .env.local (gitignored).
#
# Copy this file to .env.local and fill in your key:
#   cp .env.example .env.local

# Google Places API (New) + Maps JavaScript API
# https://console.cloud.google.com/ — enable Places API (New), Geocoding API,
# and Maps JavaScript API on the project this key belongs to.
EXPO_PUBLIC_GOOGLE_PLACES_API_KEY=
